{
  "type": "postgres",
  "host": "${POSTGRES_HOST}",
  "port": "${POSTGRES_PORT}",
  "username": "${POSTGRES_USER}",
  "password": "${POSTGRES_PASSWORD}",
  "database": "${POSTGRES_DB}",
  "entities": ["dist/**/*.entity.js"],
  "synchronize": true
}
